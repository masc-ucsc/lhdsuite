// Maximal-period 16-bit Fibonacci recurrence; bit zero is the feedback input.
// The public modules own their state and reset interfaces.
pub comb advance(state:u16) -> (next:u16) {
  const feedback:u1 = state#[15] ^ state#[13] ^ state#[12] ^ state#[10]
  next = (state#[0..=14] << 1) | feedback
}
