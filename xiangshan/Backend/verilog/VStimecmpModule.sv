// Generated by CIRCT firtool-1.150.2
module VStimecmpModule(
  input                                                  clock,
                                                         reset,
  input  struct packed {logic wen; logic [63:0] wdata; } w,
  output [63:0]                                          rdata,
  output struct packed {logic [63:0] vstimecmp; }        regOut
);

  struct packed {logic [63:0] vstimecmp; } reg_0;
  wire struct packed {logic [63:0] vstimecmp; } reg_init =
    '{vstimecmp: 64'hFFFFFFFFFFFFFFFF};
  wire struct packed {logic [63:0] vstimecmp; } wdata = '{vstimecmp: w.wdata};
  always @(posedge clock or posedge reset) begin
    if (reset)
      reg_0 <= reg_init;
    else begin
      automatic logic [63:0] _GEN;
      _GEN = w.wen ? wdata.vstimecmp : reg_0.vstimecmp;
      reg_0 <= '{vstimecmp: _GEN};
    end
  end // always @(posedge, posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    initial begin
      if (reset)
        reg_0 = reg_init;
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign rdata = reg_0.vstimecmp;
  assign regOut = reg_0;
endmodule

