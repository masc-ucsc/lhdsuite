// Generated by CIRCT firtool-1.150.2
module StoreBubbleReason(
  input        clock,
               reset,
               io_bubbleValid,
  input  [6:0] io_bubbleReason,
  input        io_redirect,
  output       io_outReasonValid,
  output [6:0] io_outReason
);

  wire       bubbleConsumed = 1'h0;
  wire
    struct packed {logic bubbleValid; logic [6:0] bubbleReason; logic redirect; logic reasonFire; logic outReasonValid; logic [6:0] outReason; }
    io;
  reg        bubbleValidReg;
  reg  [6:0] bubbleReasonReg;
  wire       canUpdate = ~bubbleValidReg;
  wire       _GEN = canUpdate & io.bubbleValid;
  wire       bubblevalidNext = ~io.redirect & (_GEN | bubbleValidReg);
  wire [6:0] _GEN_0 = _GEN ? io.bubbleReason : bubbleReasonReg;
  wire [6:0] bubbleReasonNext = io.redirect ? 7'h0 : _GEN_0;
  wire [6:0] _io_outReason_T_2 =
    io.bubbleValid & ~bubbleValidReg ? io.bubbleReason : bubbleReasonReg;
  assign io =
    '{bubbleValid: io_bubbleValid,
      bubbleReason: io_bubbleReason,
      redirect: io_redirect,
      reasonFire: (1'h0),
      outReasonValid: (bubbleValidReg | io.bubbleValid),
      outReason: _io_outReason_T_2};
  always @(posedge clock or posedge reset) begin
    if (reset)
      bubbleValidReg <= 1'h0;
    else
      bubbleValidReg <= bubblevalidNext;
  end // always @(posedge, posedge)
  always @(posedge clock or posedge reset) begin
    if (reset)
      bubbleReasonReg <= 7'h0;
    else
      bubbleReasonReg <= bubbleReasonNext;
  end // always @(posedge, posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    initial begin
      if (reset) begin
        bubbleValidReg = 1'h0;
        bubbleReasonReg = 7'h0;
      end
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_outReasonValid = io.outReasonValid;
  assign io_outReason = io.outReason;
endmodule

