// Generated by CIRCT firtool-1.150.2
module ResetGen(
  input                                                                 clock,
                                                                        reset,
  output                                                                o_reset,
  input  struct packed {logic lgc_rst_n; logic mode; logic scan_mode; } dft
);

  wire       lgc_rst = ~dft.lgc_rst_n;
  wire       real_reset = dft.mode ? lgc_rst : reset;
  reg  [2:0] pipe_reset;
  wire       raw_reset = pipe_reset[2];
  always @(posedge clock or posedge real_reset) begin
    if (real_reset)
      pipe_reset <= 3'h7;
    else
      pipe_reset <= {pipe_reset[1:0], 1'h0};
  end // always @(posedge, posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    initial begin
      if (real_reset)
        pipe_reset = 3'h7;
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign o_reset = dft.scan_mode ? lgc_rst : raw_reset;
endmodule

