// Generated by CIRCT firtool-1.150.2
module DelayNWithValid_9(
  input                                                  clock,
                                                         reset,
  input  struct packed {logic flag; logic [8:0] value; } io_in_bits,
  input                                                  io_in_valid,
  output struct packed {logic flag; logic [8:0] value; } io_out_bits,
  output                                                 io_out_valid
);

  wire
    struct packed {struct packed {logic flag; logic [8:0] value; } in_bits; logic in_valid; struct packed {logic flag; logic [8:0] value; } out_bits; logic out_valid; }
    io;
  reg  valid_REG;
  wire valid = valid_REG;
  struct packed {logic flag; logic [8:0] value; } data;
  reg  valid_REG_1;
  wire valid_1 = valid_REG_1;
  struct packed {logic flag; logic [8:0] value; } data_1;
  reg  valid_REG_2;
  wire res_valid = valid_REG_2;
  struct packed {logic flag; logic [8:0] value; } res_bits;
  assign io =
    '{in_bits: io_in_bits,
      in_valid: io_in_valid,
      out_bits: res_bits,
      out_valid: res_valid};
  always @(posedge clock or posedge reset) begin
    if (reset)
      valid_REG <= 1'h0;
    else
      valid_REG <= io.in_valid;
  end // always @(posedge, posedge)
  always @(posedge clock) begin
    automatic logic       _GEN = io.in_valid ? io.in_bits.flag : data.flag;
    automatic logic [8:0] _GEN_0 = io.in_valid ? io.in_bits.value : data.value;
    data <= '{flag: _GEN, value: _GEN_0};
  end // always @(posedge)
  always @(posedge clock or posedge reset) begin
    if (reset)
      valid_REG_1 <= 1'h0;
    else
      valid_REG_1 <= valid;
  end // always @(posedge, posedge)
  always @(posedge clock) begin
    automatic logic       _GEN_1 = valid ? data.flag : data_1.flag;
    automatic logic [8:0] _GEN_2 = valid ? data.value : data_1.value;
    data_1 <= '{flag: _GEN_1, value: _GEN_2};
  end // always @(posedge)
  always @(posedge clock or posedge reset) begin
    if (reset)
      valid_REG_2 <= 1'h0;
    else
      valid_REG_2 <= valid_1;
  end // always @(posedge, posedge)
  always @(posedge clock) begin
    automatic logic       _GEN_3 = valid_1 ? data_1.flag : res_bits.flag;
    automatic logic [8:0] _GEN_4 = valid_1 ? data_1.value : res_bits.value;
    res_bits <= '{flag: _GEN_3, value: _GEN_4};
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    initial begin
      if (reset) begin
        valid_REG = 1'h0;
        valid_REG_1 = 1'h0;
        valid_REG_2 = 1'h0;
      end
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_out_bits = io.out_bits;
  assign io_out_valid = io.out_valid;
endmodule

