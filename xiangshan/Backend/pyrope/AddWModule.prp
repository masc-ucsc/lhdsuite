pub comb AddWModule::[timecheck=false](io_src:u32, io_srcw:u32) -> (io_addw:u32) {
  mut `io.src` = io_src
  mut `io.srcw` = io_srcw
  mut `io.addw` = (`io.srcw` + `io.src`)#[0..=31]
  io_addw = `io.addw`
}
